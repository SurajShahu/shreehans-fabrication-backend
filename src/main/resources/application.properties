# ===============================
# Application Basic Config
# ===============================
spring.application.name=shree-hans-fabrication
server.port=${PORT:8080}

# ===============================
# Database Configuration (PostgreSQL - Render)
# ===============================
spring.datasource.url=jdbc:postgresql://dpg-d59a5t7gi27c73davth0-a:5432/shree_hans
spring.datasource.username=new
spring.datasource.password=qrgfV8NTeSlkG7g2FaVbw3k39qryyt2H
spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# JPA / Hibernate Configuration
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# ===============================
# Jackson (JSON Date Formatting)
# ===============================
spring.jackson.serialization.write-dates-as-timestamps=false

# ===============================
# Actuator (Health Check ? Render friendly)
# ===============================
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always


# ======================================================
# ? COMMENTED / UNWANTED (MySQL + Extra Logs)
# ======================================================

# spring.datasource.url=jdbc:mysql://${MYSQLHOST}:${MYSQLPORT}/${MYSQLDATABASE}
# spring.datasource.username=${MYSQLUSER}
# spring.datasource.password=${MYSQLPASSWORD}
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# logging.level.org.hibernate.SQL=DEBUG
# logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
